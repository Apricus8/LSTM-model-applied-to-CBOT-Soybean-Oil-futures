# LSTM-model-applied-to-CBOT-Soybean-Oil-futures
This code is similar to a listed CNN model repository - but instead uses an LSTM (Long Short-Term Memory) model instead of a CNN for time series forecasting.
